# Association model v7

Presentation-only fix over v6. `requires_review=false` is now rendered explicitly as `false` instead of an empty string. Lifecycle semantics, hashes, RDF assertions, and evaluation logic are unchanged. Existing v6 lifecycle graphs can be opened directly with the v7 tools; no experiment rerun is required.
